# 🤖 IT-Курсы Telegram Bot

Telegram-бот для подбора IT-курсов с тестированием и отслеживанием прогресса.

## ✨ Возможности

### 🎯 Для студентов:
- **Профориентационный тест** — определение подходящей IT-специальности
- **Каталог курсов** — просмотр доступных курсов с описаниями
- **Запись на курсы** — онлайн-регистрация на обучение
- **Отслеживание прогресса** — визуализация пройденных уроков
- **FAQ** — ответы на частые вопросы
- **Расписание** — даты начала курсов

### 📊 Для администраторов:
- **Аналитическая панель** — статистика по курсам и студентам
- **Интерактивные графики** — визуализация данных (Matplotlib + Seaborn)
- **Отчёты** — средний прогресс, популярные курсы

## 🛠 Технологии

- **Python 3.10+**
- **aiogram 3.x** — асинхронный Telegram Bot API
- **Matplotlib + Seaborn** — визуализация данных
- **NumPy + Pandas** — обработка данных
- **JSON** — хранение данных
- **pytest** — тестирование

## 📦 Установка

```bash
# Клонирование
git clone https://github.com/your-username/it-courses-bot.git
cd it-courses-bot

# Виртуальное окружение
python -m venv venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/macOS

# Установка зависимостей
pip install -r requirements.txt
```

## ⚙️ Настройка

Создайте `.env` файл:

```env
BOT_TOKEN=your_token_here
ADMIN_ID=your_user_id
```

**Как получить токен:**
1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

**Как узнать свой ID:**
1. Напишите [@userinfobot](https://t.me/userinfobot)
2. Скопируйте `Id`

## 🚀 Запуск

```bash
python main.py
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest

```

## 📁 Структура

```
it-courses-bot/
├── app/
│   ├── bot.py          # Инициализация бота
│   ├── config.py       # Конфигурация
│   ├── database.py     # БД
│   ├── handlers.py     # Обработчики
│   ├── keyboards.py    # Клавиатуры
│   └── states.py       # FSM состояния
├── analytics/
│   └── analyzer.py     # Аналитика и графики
├── data/
│   ├── courses.json
│   ├── users.json
│   ├── faq.json
│   ├── specialties.json
|   └── test.json
├── tests/              # Тесты
├── main.py
└── requirements.txt
```

